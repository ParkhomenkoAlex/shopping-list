{
    "name": "shopping-list",
    "main": "expo-router/entry",
    "version": "1.0.0",
    "dependencies": {
        "@supabase/supabase-js": "^2.116.0",
        "@tanstack/react-query": "^5.102.8",
        "expo": "~57.0.20",
        "expo-constants": "~57.0.17",
        "expo-linking": "~57.0.9",
        "expo-router": "~57.0.19",
        "expo-splash-screen": "~57.0.8",
        "react": "19.2.3",
        "react-native": "0.86.3",
        "react-native-gesture-handler": "~2.32.0",
        "react-native-safe-area-context": "~5.7.0",
        "react-native-screens": "~4.26.0"
    },
    "devDependencies": {
        "@types/react": "~19.2.2",
        "cross-env": "^10.1.0",
        "dotenv-cli": "^11.0.0",
        "eslint": "^9.0.0",
        "eslint-config-expo": "~57.0.2",
        "eslint-config-prettier": "^10.1.8",
        "prettier": "^3.9.6",
        "typescript": "~6.0.3"
    },
    "scripts": {
        "start": "expo start",
        "reset-project": "node ./scripts/reset-project.js",
        "ios": "expo run:ios",
        "web": "expo start --web",
        "lint": "expo lint",
        "format": "prettier --write .",
        "format:check": "prettier --check .",

        "local": "dotenv -e .env.local -- cross-env EXPO_NO_DOTENV=1 sh -c 'expo prebuild --clean --platform android && expo run:android'",
        "dev": "dotenv -e .env.development -- cross-env EXPO_NO_DOTENV=1 sh -c 'expo prebuild --clean --platform android && expo run:android'",
        "prod": "dotenv -e .env.production -- cross-env EXPO_NO_DOTENV=1 sh -c 'expo prebuild --clean --platform android && expo run:android'",

        "db:status": "supabase status",

        "db:migration:new": "supabase migration new",
        "db:migration:up": "supabase migration up",
        "db:migration:list": "supabase migration list",

        "build:apk:dev": "dotenv -e .env.development -- cross-env EXPO_NO_DOTENV=1 sh -c 'BUILD_NUMBER=${APP_BUILD_NUMBER:-200}; export APP_BUILD_NUMBER=$BUILD_NUMBER; expo prebuild --clean --platform android && cd android && ./gradlew assembleRelease && cd .. && cp android/app/build/outputs/apk/release/app-release.apk ./SL-Dev-${BUILD_NUMBER}-v1.0.0.apk'",
        "build:apk:prod": "dotenv -e .env.production -- cross-env EXPO_NO_DOTENV=1 sh -c 'BUILD_NUMBER=${APP_BUILD_NUMBER:-300}; export APP_BUILD_NUMBER=$BUILD_NUMBER; expo prebuild --clean --platform android && cd android && ./gradlew assembleRelease && cd .. && cp android/app/build/outputs/apk/release/app-release.apk ./SL-Prod-${BUILD_NUMBER}-v1.0.0.apk'",
        "env:check": "PACKAGE=$(adb shell dumpsys activity activities | grep 'ResumedActivity:' | sed -E 's/.* u0 ([a-zA-Z0-9._]+)\\/.*/\\1/'); case \"$PACKAGE\" in com.anonymous.shoppinglist.local) ENV=Local;; com.anonymous.shoppinglist.dev) ENV=Dev;; com.anonymous.shoppinglist) ENV=Prod;; *) ENV=Unknown;; esac; echo \"Environment : $ENV\"; echo \"Package     : $PACKAGE\""
    },
    "private": true
}
